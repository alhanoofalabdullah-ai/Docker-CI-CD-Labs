apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  namespace: secure-production
  name: application-reader

rules:

- apiGroups: [""]

  resources:
    - pods

  verbs:
    - get
    - list
