# Log Management Workflow

Application logs are generated by the Java application.

Logstash reads the log file from the logs directory.

Logstash parses log messages and extracts fields such as:

- Timestamp
- Log Level
- Message

Elasticsearch stores the parsed log events.

Kibana allows searching and visualizing the logs.
