# Infrastructure Workflow

Terraform allows infrastructure to be managed through code.

The workflow starts by defining infrastructure resources inside configuration files.

Terraform then creates an execution plan and applies the required changes.

This improves consistency, repeatability, governance, and automation.
