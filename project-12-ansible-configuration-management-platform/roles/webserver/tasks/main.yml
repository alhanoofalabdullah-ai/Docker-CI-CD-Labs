---
- name: Install Apache

  package:
    name: httpd
    state: present

- name: Start Apache

  service:
    name: httpd
    state: started
    enabled: true

- name: Deploy Homepage

  template:
    src: index.html.j2
    dest: /var/www/html/index.html

  notify:
    - Restart Apache
