# Deployment Workflow

Infrastructure is defined using Terraform.

Terraform provisions Azure resources automatically.

Resources are deployed consistently across environments.

Changes are version controlled and repeatable.
