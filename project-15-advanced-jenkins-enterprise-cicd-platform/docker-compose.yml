version: '3.9'

services:

  jenkins:

    image: jenkins/jenkins:lts

    container_name: jenkins-server

    ports:
      - "8080:8080"

  sonarqube:

    image: sonarqube:lts

    container_name: sonarqube

    ports:
      - "9000:9000"
